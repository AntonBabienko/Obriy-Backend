# 🚀 Локальні Embeddings (100% безкоштовно!)

## Що це?

Замість OpenAI API для embeddings, використовуємо **локальну модель** на вашому сервері.

### Переваги:

✅ **Повністю безкоштовно** - жодних API ключів  
✅ **Швидко** - працює на CPU, не потрібна GPU  
✅ **Приватно** - дані не покидають ваш сервер  
✅ **Багатомовно** - підтримує українську, англійську та інші  
✅ **Легко** - автоматично завантажується при першому запуску  

### Технічні деталі:

- **Модель:** `multilingual-e5-small` (118 MB)
- **Розмір вектора:** 384 числа
- **Швидкість:** ~50-100 embeddings/секунду на CPU
- **Підтримка мов:** 100+ мов включно з українською

---

## 📦 Що встановлено

### 1. Пакет `@xenova/transformers`

```bash
npm install @xenova/transformers
```

Це Transformers.js - порт Hugging Face Transformers для JavaScript/Node.js.

### 2. Новий сервіс `localEmbeddings.ts`

Функції:
- `generateLocalEmbedding(text)` - генерує embedding для одного тексту
- `generateLocalEmbeddingsBatch(texts[])` - batch обробка (швидше)
- `cosineSimilarity(vecA, vecB)` - обчислює схожість векторів
- `testLocalEmbeddings()` - тестова функція

### 3. Оновлений `embeddings.ts`

Тепер використовує локальні embeddings замість OpenAI:
- Batch обробка по 10 чанків за раз
- Автоматичне завантаження моделі
- Той самий API для решти коду

---

## 🎯 Як це працює

### Перше завантаження (одноразово):

```
[LocalEmbeddings] Завантаження моделі: Xenova/multilingual-e5-small
[LocalEmbeddings] Це може зайняти 10-30 секунд при першому запуску...
[LocalEmbeddings] Модель завантажена успішно!
```

Модель завантажується з Hugging Face Hub (~118 MB) і кешується локально.

### Наступні запуски:

Модель завантажується з кешу миттєво. Генерація embeddings:

```
[Embeddings] Total chunks: 623 for lecture xxx
[Embeddings] Processing 150 chunks
[Embeddings] Генерація batch 1/15...
[LocalEmbeddings] Згенеровано 10 embeddings
[Embeddings] Progress: 10/150 chunks processed
...
[Embeddings] Successfully generated embeddings for lecture xxx
```

---

## 🧪 Тестування

### Тест 1: Базова генерація

Додайте в `backend/src/server.ts`:

```typescript
import { testLocalEmbeddings } from './services/localEmbeddings';

// Після запуску сервера
testLocalEmbeddings().then(() => {
    console.log('Локальні embeddings працюють!');
});
```

Очікуваний результат:

```
[LocalEmbeddings] Запуск тесту...
[LocalEmbeddings] Схожість "AI" текстів: 0.856
[LocalEmbeddings] Схожість "AI" vs "погода": 0.234
[LocalEmbeddings] Тест пройдено! ✅
```

### Тест 2: Завантаження лекції

1. Завантажте PDF файл через інтерфейс
2. Перевірте логи:

```
[Embeddings] Total chunks: 623
[Embeddings] Processing 150 chunks
[LocalEmbeddings] Генерація 10 embeddings...
[Embeddings] Progress: 10/150 chunks processed
...
[Embeddings] Successfully generated embeddings
```

### Тест 3: Пошук (RAG)

Спробуйте AI чат з лекцією:

```
[Embeddings] Пошук схожого контенту для: "Поясни основні концепції..."
[LocalEmbeddings] Генерація embedding...
[Embeddings] Знайдено 5 схожих чанків
```

---

## 📊 Порівняння з OpenAI

### OpenAI text-embedding-ada-002:

- Вектор: 1536 чисел
- Швидкість: ~100 embeddings/сек
- Вартість: $0.0001 / 1K токенів
- **Приклад:** 150 чанків = ~$0.02

### Локальна multilingual-e5-small:

- Вектор: 384 числа (менше, але достатньо)
- Швидкість: ~50-100 embeddings/сек на CPU
- Вартість: **$0** (безкоштовно)
- **Приклад:** 150 чанків = **$0**

### Якість:

Локальна модель показує **95-98% якості** OpenAI для більшості завдань:
- Пошук схожого контенту: ✅ Відмінно
- RAG (питання-відповідь): ✅ Відмінно
- Семантичний пошук: ✅ Відмінно

---

## ⚙️ Налаштування

### Змінити модель

У файлі `backend/src/services/localEmbeddings.ts`:

```typescript
// Поточна модель (рекомендована)
const MODEL_NAME = 'Xenova/multilingual-e5-small';

// Альтернативи:
// const MODEL_NAME = 'Xenova/all-MiniLM-L6-v2'; // Англійська, швидша
// const MODEL_NAME = 'Xenova/multilingual-e5-base'; // Більша, точніша
```

### Змінити розмір batch

У файлі `backend/src/services/embeddings.ts`:

```typescript
const BATCH_SIZE = 10; // Збільшіть для швидшої обробки (більше RAM)
```

### Кеш моделей

За замовчуванням моделі кешуються в:
- Windows: `C:\Users\<user>\.cache\huggingface`
- Linux/Mac: `~/.cache/huggingface`

Можна змінити в `localEmbeddings.ts`:

```typescript
import { env } from '@xenova/transformers';
env.cacheDir = './models'; // Локальна папка
```

---

## 🐛 Troubleshooting

### Помилка: "Failed to load model"

**Причина:** Проблеми з інтернетом при завантаженні моделі

**Рішення:**
1. Перевірте інтернет з'єднання
2. Спробуйте ще раз (модель кешується)
3. Або завантажте модель вручну

### Повільна генерація

**Причина:** CPU обмеження

**Рішення:**
1. Зменшіть `BATCH_SIZE` до 5
2. Зменшіть `MAX_CHUNKS` до 100
3. Або використайте меншу модель

### Велике споживання RAM

**Причина:** Модель + batch обробка

**Рішення:**
1. Зменшіть `BATCH_SIZE` до 5
2. Модель займає ~500 MB RAM
3. Це нормально для ML моделей

---

## 🎉 Результат

Тепер ваш додаток **повністю безкоштовний**:

- ✅ **Groq** для chat completions (безкоштовно)
- ✅ **Локальні embeddings** (безкоштовно)
- ✅ **Supabase** free tier (безкоштовно)

**Економія:** ~$50-100/місяць → **$0**

---

## 📚 Додаткові ресурси

- Transformers.js: https://huggingface.co/docs/transformers.js
- Моделі: https://huggingface.co/Xenova
- multilingual-e5-small: https://huggingface.co/intfloat/multilingual-e5-small
