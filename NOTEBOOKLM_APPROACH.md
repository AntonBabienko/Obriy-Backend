# Підхід NotebookLM для обробки документів

## Як працює NotebookLM

### 1. Повна обробка документа
- **Gemini 1.5 Pro** з контекстним вікном 1M+ токенів
- Може обробити цілу книгу за раз
- Створює "карту знань" документа

### 2. Багаторівневе індексування
```
Документ
├── Розділи (chapters)
│   ├── Підрозділи (sections)
│   │   ├── Параграфи (paragraphs)
│   │   │   └── Речення (sentences)
```

### 3. Семантичні зв'язки
- Розуміє контекст і зв'язки
- Може відповідати на питання про весь документ
- Генерує summaries та insights

## Наша поточна реалізація

### Що ми маємо:
```typescript
// Семплювання для великих файлів
const chunks = sampleChunks(allChunks, MAX_CHUNKS);
// Embeddings для вибірки
generateEmbeddings(lectureId, chunks);
```

### Обмеження:
- ❌ Не бачимо весь документ одночасно
- ❌ Можемо пропустити важливі зв'язки
- ❌ Семплювання може пропустити ключову інформацію

## Як покращити до рівня NotebookLM

### Варіант 1: Використати Gemini (як NotebookLM)
```typescript
import { GoogleGenerativeAI } from '@google/generative-ai';

const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY);
const model = genAI.getGenerativeModel({ 
  model: 'gemini-1.5-pro',
  generationConfig: {
    maxOutputTokens: 8192,
  }
});

// Gemini може обробити до 1M токенів контексту
const result = await model.generateContent([
  'Проаналізуй цей документ і створи структуровану базу знань',
  fullDocumentText
]);
```

**Переваги:**
- ✅ Обробляє весь документ
- ✅ Розуміє контекст
- ✅ Може генерувати summaries

**Недоліки:**
- ❌ Дорого (Gemini API)
- ❌ Повільно для великих документів
- ❌ Потребує Google Cloud

### Варіант 2: Ієрархічне індексування (рекомендовано)
```typescript
// 1. Розбити документ на логічні секції
interface DocumentStructure {
  title: string;
  sections: Section[];
}

interface Section {
  heading: string;
  content: string;
  subsections: Section[];
  embeddings: number[];
}

// 2. Створити embeddings для кожної секції
async function indexDocument(content: string) {
  const structure = parseDocumentStructure(content);
  
  for (const section of structure.sections) {
    // Embedding для всієї секції
    const sectionEmbedding = await generateEmbedding(section.content);
    
    // Зберегти з метаданими
    await supabase.from('lecture_sections').insert({
      lecture_id: lectureId,
      heading: section.heading,
      content: section.content,
      embedding: sectionEmbedding,
      level: section.level
    });
  }
}

// 3. Двоетапний пошук
async function smartSearch(query: string) {
  // Крок 1: Знайти релевантні секції
  const relevantSections = await findSimilarSections(query);
  
  // Крок 2: Шукати в межах цих секцій
  const detailedResults = await searchWithinSections(
    query, 
    relevantSections
  );
  
  return detailedResults;
}
```

**Переваги:**
- ✅ Зберігає структуру документа
- ✅ Швидший пошук
- ✅ Краще розуміння контексту
- ✅ Працює з OpenAI embeddings

### Варіант 3: Гібридний підхід (найкраще співвідношення)
```typescript
// Комбінація семплювання + ієрархічне індексування

async function hybridIndexing(content: string) {
  // 1. Створити структуру
  const structure = parseDocumentStructure(content);
  
  // 2. Для кожної секції:
  for (const section of structure.sections) {
    if (section.content.length < 5000) {
      // Маленькі секції - повністю
      await indexFullSection(section);
    } else {
      // Великі секції - семплювання
      const samples = sampleContent(section.content);
      await indexSampledSection(section, samples);
    }
  }
  
  // 3. Створити summary всього документа
  const summary = await generateDocumentSummary(structure);
  await storeSummary(lectureId, summary);
}
```

## Рекомендована архітектура

```
┌─────────────────────────────────────┐
│         Завантаження файлу          │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│    Витягування тексту (PDF/DOCX)   │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   Парсинг структури документа       │
│   (розділи, підрозділи, параграфи)  │
└──────────────┬──────────────────────┘
               │
               ├──────────────┬──────────────┐
               ▼              ▼              ▼
        ┌──────────┐   ┌──────────┐   ┌──────────┐
        │ Summary  │   │ Sections │   │  Chunks  │
        │ Embedding│   │Embeddings│   │Embeddings│
        └──────────┘   └──────────┘   └──────────┘
               │              │              │
               └──────────────┴──────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │  Vector Database │
                    │    (Supabase)    │
                    └──────────────────┘
```

## Наступні кроки для покращення

### Фаза 1: Базове покращення (1-2 дні)
1. Додати генерацію summary для кожного документа
2. Зберігати метадані (розмір, тип, дата)
3. Покращити UI для відображення структури

### Фаза 2: Ієрархічне індексування (3-5 днів)
1. Парсинг структури документа
2. Створення embeddings для секцій
3. Двоетапний пошук

### Фаза 3: AI-покращення (1 тиждень)
1. Автоматична генерація питань
2. Ключові концепції
3. Зв'язки між документами

## Порівняння підходів

| Функція | Наша поточна | Варіант 2 | NotebookLM |
|---------|--------------|-----------|------------|
| Весь документ | ❌ Семплювання | ✅ Структура | ✅ Повністю |
| Швидкість | ⚡ Швидко | ⚡ Швидко | 🐌 Повільно |
| Вартість | 💰 Дешево | 💰 Дешево | 💰💰 Дорого |
| Точність | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Складність | 🔧 Просто | 🔧🔧 Середньо | 🔧🔧🔧 Складно |

## Висновок

Для досягнення якості NotebookLM рекомендую:
1. **Короткостроково**: Залишити поточний підхід з семплюванням
2. **Середньостроково**: Додати ієрархічне індексування (Варіант 2)
3. **Довгостроково**: Розглянути Gemini API для критичних документів
